import React from "react";
import styled from "styled-components";

const MainTitle = () => {
  return <Title>NEW ARRIVAL</Title>;
};

const Title = styled.div`
  padding: 4rem 0 3rem 0;
  font-size: 2rem;
  letter-spacing: 0.5rem;
  text-align: center;
`;

export default MainTitle;
